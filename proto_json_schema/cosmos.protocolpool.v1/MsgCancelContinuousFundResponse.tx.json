{
    "$schema": "http://json-schema.org/draft-04/schema#",
    "$ref": "#/definitions/MsgCancelContinuousFundResponse",
    "definitions": {
        "MsgCancelContinuousFundResponse": {
            "properties": {
                "canceled_time": {
                    "type": "string",
                    "description": "CanceledTime is the canceled time.",
                    "format": "date-time"
                },
                "canceled_height": {
                    "type": "string",
                    "description": "CanceledHeight defines the canceled block height."
                },
                "recipient": {
                    "type": "string",
                    "description": "Recipient is the account address string of the recipient whose funds are cancelled."
                }
            },
            "additionalProperties": true,
            "type": "object",
            "title": "Msg Cancel Continuous Fund Response",
            "description": "MsgCancelContinuousFundResponse defines the response to executing a MsgCancelContinuousFund message."
        }
    }
}