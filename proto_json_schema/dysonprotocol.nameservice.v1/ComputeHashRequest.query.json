{
    "$schema": "http://json-schema.org/draft-04/schema#",
    "$ref": "#/definitions/ComputeHashRequest",
    "definitions": {
        "ComputeHashRequest": {
            "properties": {
                "name": {
                    "type": "string",
                    "description": "name is the name to compute the hash for"
                },
                "salt": {
                    "type": "string",
                    "description": "salt is the salt to use for the hash computation"
                },
                "committer": {
                    "type": "string",
                    "description": "committer is the committer address"
                }
            },
            "additionalProperties": true,
            "type": "object",
            "title": "Compute Hash Request",
            "description": "ComputeHashRequest is a request to compute the hash for a name and salt"
        }
    }
}