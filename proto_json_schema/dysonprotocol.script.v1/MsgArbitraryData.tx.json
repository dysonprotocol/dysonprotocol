{
    "$schema": "http://json-schema.org/draft-04/schema#",
    "$ref": "#/definitions/MsgArbitraryData",
    "definitions": {
        "MsgArbitraryData": {
            "properties": {
                "signer": {
                    "type": "string",
                    "description": "signer is the account address signing the data"
                },
                "data": {
                    "type": "string",
                    "description": "data is the arbitrary data being signed"
                },
                "app_domain": {
                    "type": "string",
                    "description": "app_domain is an optional application domain for the data"
                }
            },
            "additionalProperties": true,
            "type": "object",
            "title": "Msg Arbitrary Data",
            "description": "MsgArbitraryData is used for signing arbitrary data for verification purposes"
        }
    }
}