{
    "$schema": "http://json-schema.org/draft-04/schema#",
    "$ref": "#/definitions/MsgCancelProposalResponse",
    "definitions": {
        "MsgCancelProposalResponse": {
            "properties": {
                "proposal_id": {
                    "type": "string",
                    "description": "proposal_id defines the unique id of the proposal."
                },
                "canceled_time": {
                    "type": "string",
                    "description": "canceled_time is the time when proposal is canceled.",
                    "format": "date-time"
                },
                "canceled_height": {
                    "type": "string",
                    "description": "canceled_height defines the block height at which the proposal is canceled."
                }
            },
            "additionalProperties": true,
            "type": "object",
            "title": "Msg Cancel Proposal Response",
            "description": "MsgCancelProposalResponse defines the response structure for executing a MsgCancelProposal message."
        }
    }
}